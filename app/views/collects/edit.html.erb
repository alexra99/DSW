<% content_for :title, "Edit note" %>

  <div class="edit">

    <div class="edit_info">
      <%#= image_tag  "ruby-red.png", class: "edit_img", alt: "Your avatar" %>
      <span>Edit Collect</span>
    </div>

    <%= form_for @collect, url: { action: 'update' } do |f| %>
      <div class="eu_div">
        <span class='edit_span'>Title</span>
        <%= f.text_field :name, class: "e_username", placeholder: 'Title..', autofocus: true, spellcheck:false, autocomplete: false, :required => true %>
        <% @collect.errors.full_messages_for(:name).each do |message| %>
          <div><%= message %></div>
        <% end %>
      </div>

      <div class="note_actions">
        <%= link_to "Cancel Collect", collects_path, class: "cancel_btn" %>
        <%= f.submit "Update Collect", class: "btn" %>
      </div>
    <% end %>

  </div>
